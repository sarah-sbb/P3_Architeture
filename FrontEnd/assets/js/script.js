import { initGallery } from './gallery.js';
import { initModal } from './modal.js';
// import { initForm } from './formModal.js';

document.addEventListener('DOMContentLoaded', () => {
    initGallery();
    initModal();
    // initForm();
});